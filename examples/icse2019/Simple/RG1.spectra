// basic alur example

module Alur
env boolean r;
env	boolean c;

sys boolean g;
sys boolean v;



gar
	G(r -> F(next(g)));
	
gar ungrant:
	G ((c | g) ->next(!g));
gar cnv:
	G (c -> !v);
gar gnv:
	GF g & v;
